# Runtime dependencies.
#
# Pinned exactly so a Lambda build is reproducible: an unpinned floor means two
# deploys a week apart can ship different code. Dependabot proposes bumps, and CI
# runs the full suite against each one before it merges.
#
# Note: the Python Lambda runtime already provides boto3. It is pinned here so the
# local and CI environments match what tests were written against, rather than to
# add it to the deployment package.
boto3==1.43.56
